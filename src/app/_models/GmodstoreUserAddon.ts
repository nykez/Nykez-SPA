
export interface GmodstoreUserAddon {
    id: string,
    teamId: string,
    active: boolean,
    name: string,
    short_description: string,
    description: string,
    images: [],
    slug: string,
    createdAt: Date,
    updatedAt: Date,
    route: string,
}